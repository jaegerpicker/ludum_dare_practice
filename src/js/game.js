(function() {
  'use strict';

  function Game() {}

  Game.prototype = {
    create: function () {
      this.input.onDown.add(this.onInputDown, this);
    },

    update: function () {

    },

    onInputDown: function () {
      this.game.state.start('menu');
    }
  };

  window['hack-the-planet'] = window['hack-the-planet'] || {};
  window['hack-the-planet'].Game = Game;
}());
